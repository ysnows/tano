# Task 005: CLI Tooling

**Status**: MOSTLY COMPLETE
**Phase**: 5 — CLI Tooling
**Completed**: 2026-03-21

## Implemented Commands

| Command | Status |
|---------|--------|
| `tano create <name>` | Done — scaffolds from template |
| `tano doctor` | Done — checks 8 environment requirements |
| `tano dev` | Done — Bun server + simulator |
| `tano build ios` | Done — Bun.build + xcodebuild |
| `tano run ios` | Done — build + install + launch |
| `tano build android` | Stub (Phase 6) |
| `tano run android` | Stub (Phase 6) |
| `tano plugin add/create` | Stub |

## Remaining
- Android commands (after Phase 6)
- `tano plugin add` / `tano plugin create`
- HMR / hot reload for web assets
