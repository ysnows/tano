{
    "name": "{{APP_NAME_LOWER}}",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev:server": "bun run src/server/index.ts",
        "dev:web": "cd src/web && npx vite dev",
        "build:server": "bun build src/server/index.ts --outdir dist/server --minify",
        "build:web": "cd src/web && npx vite build --outDir ../../dist/web"
    },
    "dependencies": {
        "@tano/bridge": "^0.1.0"
    },
    "devDependencies": {
        "vue": "^3.4.0",
        "@vitejs/plugin-vue": "^5.0.0",
        "vite": "^5.0.0",
        "typescript": "^5.0.0"
    }
}
